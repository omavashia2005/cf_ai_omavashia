### Claude Sonnet (via web app) to modify `app.tsx` UI
Redesign the user interface to be cleaner, more modern, and visually appealing without altering any of the existing functionality or underlying logic. Focus solely on improving layout, styling, and overall user experience.

```js
// app.tsx 
```

---

### ChatGPT 5 (via web app) to parse JSON correctly in TS
Given the console output below, modify the code so that it parses and displays only the course names (keys in the JSON object) when executing console.log(response.content). Do not alter any other logic or functionality, focusing solely on extracting and printing the course names.
```json
// json object as string
```